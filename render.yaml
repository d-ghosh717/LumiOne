services:
  - type: web
    name: lumione-backend
    env: node
    plan: free
    rootDir: backend
    buildCommand: npm install && bash render-build.sh
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
