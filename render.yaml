services:
  - type: web
    name: lumione-backend
    env: node
    plan: free
    rootDir: backend
    buildCommand: npm install && pip3 install -U yt-dlp
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
