services:
  - type: web
    name: lumione-backend
    env: node
    plan: free
    rootDir: backend
    buildCommand: npm install && pip install -r requirements.txt
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
